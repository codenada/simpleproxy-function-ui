{
  "name": "simpleproxy-function-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --loader ./test/text-template-loader.mjs --test test/worker.smoke.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  },
  "dependencies": {
    "jsonata": "^2.0.6",
    "yaml": "^2.8.1"
  }
}
